
class GridChallenge:

	def __init__(self, grid):
		self.grid = grid

	def sort_rows(self):

		for row, seq in enumerate(grid):
			grid[row] = sorted(seq)

		for col 




if __name__ == "__main__":

	grid = [['e', 'a', 'h', 'b'],
			['']
